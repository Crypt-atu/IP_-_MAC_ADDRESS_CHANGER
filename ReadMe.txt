Author: Crypt
Description: This is just a basic script that generate an ip_addr(private) and a mac_addr,
then change the system ip_addr and mac_addr based on the generated IP and MAC.

Note; This works on you LAN(Local Area Network)